# AGENTS.md

## Scope

This is a résumé repo. Minimal changes only.

## Rules

- Single source of truth: `resume.json`
- Don't modify `docs/index.html` — auto-generated by CI
- Follow JSON Resume schema for all data changes
- Keep dependencies minimal — one theme dep is enough
- CI pipeline: push to `main` triggers HTML export
